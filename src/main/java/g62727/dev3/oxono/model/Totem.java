package g62727.dev3.oxono.model;

/**
 * Class representing a totem in the game
 * Characterized only by a symbol
 */
public class Totem extends Pawn {
    /**
     * Constructor to instantiate a new totem thanks to the parent class Pawn
     *
     * @param symbol - the symbol of the totem
     */
    Totem(Symbol symbol) {
        super(symbol);
    }
}
